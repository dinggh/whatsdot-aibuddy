<view class="screen">
  <view class="status-bar"><text>9:41</text><text>⋯</text></view>

  <view class="content">
    <view class="user">
      <image wx:if="{{user.avatarUrl}}" src="{{user.avatarUrl}}" class="avatar" mode="aspectFill" />
      <view wx:else class="avatar">👤</view>
      <view>
        <text class="name">{{user.nickName}}</text>
        <text class="child">{{user.phoneNumber ? ('手机号：' + user.phoneNumber) : '未绑定手机号'}}</text>
      </view>
    </view>

    <view class="actions">
      <button class="auth-btn panel" bindtap="onTapSyncProfile">同步昵称头像</button>
      <button class="auth-btn panel" open-type="getPhoneNumber" bindgetphonenumber="onGetPhoneNumber">绑定手机号</button>
    </view>

    <view class="stats">
      <view class="panel stat"><text class="num g">{{user.usedCount}}</text><text class="label">已用题量</text></view>
      <view class="panel stat"><text class="num o">{{user.remainingCount}}</text><text class="label">剩余次数</text></view>
    </view>

    <view class="buy"><text class="buy-title">购买题包</text><text class="buy-sub">100次 / 月 · 限时优惠</text></view>

    <view class="panel menu">
      <view class="menu-item"><text>家长成长指南</text><text>›</text></view>
      <view class="menu-item"><text>辅导设置</text><text>›</text></view>
      <view class="menu-item"><text>意见反馈</text><text>›</text></view>
      <view class="menu-item"><text>关于我们</text><text>›</text></view>
    </view>
  </view>

  <view class="tabbar">
    <view class="tab" bindtap="goHome"><text>首页</text></view>
    <view class="tab" bindtap="goHistory"><text>历史记录</text></view>
    <view class="tab"><text>家长课堂</text></view>
    <view class="tab active"><text>我的</text></view>
  </view>
</view>
